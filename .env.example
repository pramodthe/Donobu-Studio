# MeetNira target (override for staging)
MEETNIRA_BASE_URL=https://meetnira.com

# Run browsers without UI (default is headed locally; CI is always headless)
# MEETNIRA_HEADLESS=1

# Optional — Donobu Studio / AI features (https://www.donobu.com/docs/playwright/ai-providers)
# DONOBU_API_KEY=
